import User from '../models/User.js'; import { verifyToken } from '../config/jwt.js'; import { AppError } from '../utils/apiResponse.js';
export const authenticate = async (req, _res, next) => { try { const [scheme, token] = (req.headers.authorization || '').split(' '); if (scheme !== 'Bearer' || !token) throw new AppError(401, 'Authentication required', 'AUTH_REQUIRED'); const payload = verifyToken(token); const user = await User.findById(payload.sub); if (!user || user.status !== 'active') throw new AppError(401, 'Invalid or inactive session', 'INVALID_TOKEN'); req.user = user; next(); } catch (error) { next(error.statusCode ? error : new AppError(401, 'Invalid or expired token', 'INVALID_TOKEN')); } };
