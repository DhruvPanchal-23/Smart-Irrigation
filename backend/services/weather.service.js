import axios from 'axios'; import WeatherHistory from '../models/WeatherHistory.js'; import { AppError } from '../utils/apiResponse.js';
export const normalizeCurrentWeather = (data) => ({ temperature: data.main?.temp, humidity: data.main?.humidity, windSpeed: data.wind?.speed, pressure: data.main?.pressure, rainProbability: Math.round((data.pop || 0) * 100), condition: data.weather?.[0]?.description || 'Unknown', icon: data.weather?.[0]?.icon || '', recordedAt: new Date() });
export const fetchCurrentWeather = async (latitude, longitude) => { if (!process.env.OPENWEATHER_API_KEY) throw new AppError(503, 'Weather service is not configured', 'WEATHER_NOT_CONFIGURED'); try { const { data } = await axios.get(`${process.env.OPENWEATHER_BASE_URL || 'https://api.openweathermap.org'}/data/2.5/weather`, { params: { lat: latitude, lon: longitude, units: 'metric', appid: process.env.OPENWEATHER_API_KEY }, timeout: 8000 }); return normalizeCurrentWeather(data); } catch (error) { if (error.code === 'ECONNABORTED') throw new AppError(504, 'Weather provider timed out', 'WEATHER_TIMEOUT'); throw new AppError(502, 'Weather provider is unavailable', 'WEATHER_PROVIDER_ERROR'); } };
export const storeWeather = (user, farm, weather) => WeatherHistory.create({ user, farm, ...weather });
