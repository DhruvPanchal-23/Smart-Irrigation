import api from './api.js'; export const getFarms = (params) => api.get('/farms', { params }).then((r) => r.data.data); export const getFarmById = (id) => api.get(`/farms/${id}`).then((r) => r.data.data); export const createFarm = (data) => api.post('/farms', data).then((r) => r.data.data); export const updateFarm = (id, data) => api.put(`/farms/${id}`, data).then((r) => r.data.data); export const deleteFarm = (id) => api.delete(`/farms/${id}`).then((r) => r.data);
