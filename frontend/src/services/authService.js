import api from './api.js';
export const register = (data) => api.post('/auth/register', data).then((r) => r.data.data); export const login = (data) => api.post('/auth/login', data).then((r) => r.data.data); export const getCurrentUser = () => api.get('/auth/me').then((r) => r.data.data); export const getProfile = () => api.get('/users/profile').then((r) => r.data.data); export const updateProfile = (data) => api.put('/users/profile', data).then((r) => r.data.data); export const changePassword = (data) => api.put('/users/change-password', data).then((r) => r.data); export const logout = () => api.post('/auth/logout').catch(() => null);
