import { useEffect,useState } from 'react'; import { Link,useNavigate,useParams } from 'react-router-dom'; import * as farms from '../services/farmService.js'; import Loader from '../components/common/Loader.jsx'; export default function FarmDetails(){const {farmId}=useParams(),navigate=useNavigate(),[farm,setFarm]=useState(null);useEffect(()=>{farms.getFarmById(farmId).then(setFarm);},[farmId]);if(!farm)return <Loader/>;const remove=async()=>{if(window.confirm('Delete this farm and all of its history?')){await farms.deleteFarm(farmId);navigate('/farms');}};return <><div className="page-head"><div><span className="eyebrow">Farm</span><h1>{farm.farmName}</h1></div><button className="danger" onClick={remove}>Delete</button></div><div className="grid three"><article className="card"><h2>Crop</h2><p>{farm.cropType}</p></article><article className="card"><h2>Area</h2><p>{farm.area} {farm.areaUnit}</p></article><article className="card"><h2>Location</h2><p>{farm.location.latitude}, {farm.location.longitude}</p></article></div><div className="actions"><Link className="button" to={'/weather?farm='+farmId}>Check weather</Link><Link className="button secondary" to={'/recommendation?farm='+farmId}>Get recommendation</Link></div></>;}
