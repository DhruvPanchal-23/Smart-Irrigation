import { useState } from 'react'; import { Link, useNavigate } from 'react-router-dom'; import useAuth from '../hooks/useAuth.js'; import Alert from '../components/common/Alert.jsx';
export default function Login() { const auth = useAuth(), navigate = useNavigate(); const [form,setForm] = useState({email:'',password:''}), [error,setError] = useState(''), [busy,setBusy] = useState(false); const submit = async (e) => { e.preventDefault(); setBusy(true); setError(''); try { await auth.login(form); navigate('/dashboard'); } catch (x) { setError(x.response?.data?.message || 'Login failed'); } finally { setBusy(false); } }; return <section className="auth-page"><form className="form-card" onSubmit={submit}><span className="eyebrow">Welcome back</span><h1>Sign in</h1><Alert>{error}</Alert><label>Email<input type="email" value={form.email} onChange={(e)=>setForm({...form,email:e.target.value})} required /></label><label>Password<input type="password" value={form.password} onChange={(e)=>setForm({...form,password:e.target.value})} required /></label><button className="button" disabled={busy}>{busy?'Signing in...':'Sign in'}</button><p>New here? <Link to="/register">Create an account</Link></p></form></section>; }
