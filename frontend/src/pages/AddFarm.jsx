import { useState } from 'react'; import { useNavigate } from 'react-router-dom'; import FarmForm from '../components/farm/FarmForm.jsx'; import * as farms from '../services/farmService.js';
export default function AddFarm(){const [busy,setBusy]=useState(false),navigate=useNavigate();const save=async(data)=>{setBusy(true);try{const farm=await farms.createFarm(data);navigate('/farms/'+farm._id);}finally{setBusy(false);}};return <><div className="page-head"><div><span className="eyebrow">Farm setup</span><h1>Add a farm</h1><p>Add its crop, size, and exact coordinates.</p></div></div><FarmForm onSubmit={save} busy={busy}/></>;}
