import { useState } from 'react'; import { Link, useNavigate } from 'react-router-dom'; import useAuth from '../hooks/useAuth.js'; import Alert from '../components/common/Alert.jsx';
export default function Register() { const auth=useAuth(), navigate=useNavigate(); const [form,setForm]=useState({name:'',email:'',mobile:'',password:''}), [error,setError]=useState(''), [busy,setBusy]=useState(false); const change=(e)=>setForm({...form,[e.target.name]:e.target.value}); const submit=async(e)=>{e.preventDefault();setBusy(true);setError('');try{await auth.register(form);navigate('/dashboard');}catch(x){setError(x.response?.data?.message||'Registration failed');}finally{setBusy(false);}}; return <section className="auth-page"><form className="form-card" onSubmit={submit}><span className="eyebrow">Start smart</span><h1>Create account</h1><Alert>{error}</Alert><label>Name<input name="name" value={form.name} onChange={change} minLength="2" required /></label><label>Email<input name="email" type="email" value={form.email} onChange={change} required /></label><label>Mobile<input name="mobile" value={form.mobile} onChange={change} /></label><label>Password<input name="password" type="password" value={form.password} onChange={change} minLength="8" required /></label><button className="button" disabled={busy}>{busy?'Creating...':'Create account'}</button><p>Already registered? <Link to="/login">Sign in</Link></p></form></section>; }
