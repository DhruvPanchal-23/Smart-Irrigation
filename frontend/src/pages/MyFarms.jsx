import { Link } from 'react-router-dom'; import useFarm from '../hooks/useFarm.js'; import FarmCard from '../components/farm/FarmCard.jsx'; import Loader from '../components/common/Loader.jsx'; import Alert from '../components/common/Alert.jsx'; export default function MyFarms(){const {items,loading,error}=useFarm();return <><div className="page-head"><h1>My farms</h1><Link className="button" to="/farms/add">Add farm</Link></div><Alert>{error}</Alert>{loading?<Loader/>:<div className="grid three">{items.map((f)=><FarmCard key={f._id} farm={f}/>)}{!items.length&&<div className="empty card">No farms found.</div>}</div>}</>;}
